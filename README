
urxvt config

gt-tabbed:
  It is derived from the default tabbed extension but the hotkeys are changed
  to the gnome-terminal style.  Use Ctrl-Shift-T to create a new tab and use
  Alt-[1-9] to navigate.

tabbed:
  Shift-down:creat new tab
  Shift-left/right: move between tabs
  Alt-[1-9] navigate using tab number
  Alt-left/right: move tab around


Usage:

Use this repo's `tabbed` file to replace `/usr/lib/urxvt/perl/tabbed`
